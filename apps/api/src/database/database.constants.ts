/** Token de inyección del `Pool` de `pg`. */
export const PG_POOL = Symbol('PG_POOL');

/** Token de inyección de la instancia de Drizzle (`Database`). */
export const DATABASE = Symbol('DATABASE');
